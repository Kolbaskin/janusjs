tinymce.PluginManager.add('video', function(editor) {
    editor.addButton('video', {
    	icon: 'media',
        title: D.t('Insert Youtube video'),
		onclick: function() {
            var code = prompt(D.t('Enter youtube video code'), '')
            if(code) {
                editor.setContent(editor.getContent() + Ext.create('Ext.XTemplate', editor.userCfg.video.tpl).compile().apply({code: code}));    
            }
		}
	});

/*
	editor.addMenuItem('imageext', {
		icon: 'image',
		text: 'Insert image',
		onclick: createImageList(showDialog),
		context: 'insert',
		prependToContext: true
	});
*/
	//editor.addCommand('mceImageExt', createImageList(showDialog));
});

